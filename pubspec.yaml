name: flutter_network_logger
description: >
  A Flutter package that intercepts and logs HTTP network requests via
  HttpOverrides, with an in-app overlay and full-screen log viewer for
  inspecting request/response details.
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
